#include "main.h"


/**
 *A function that adds two ints and returns sum as result
 *add: adds two int
 *@x: is first value
 *@y: is second value
 *Return: sum
 */

int add(int x, int y)
{
int sum;
sum = x + y;
return (sum);
}
